# 🤡.fm

🤡.fm 的 GitHub Pages 首页——一家只播放荒诞的 `FM 92.1`。

## 本地预览

这是一个零构建依赖的静态站点，直接用任意静态服务器打开即可：

```bash
python3 -m http.server 8080
```

然后访问 <http://localhost:8080>。

## GitHub Pages

仓库已包含 `CNAME`，其中 `xn--9p9h.fm` 是 `🤡.fm` 的 Punycode 形式。将 Pages 的发布来源设为仓库根目录，并按 GitHub 提示配置 DNS 即可。
